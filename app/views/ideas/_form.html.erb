<%= form_for (@idea) do |f| %>
  <%= f.text_field :name %>

  <%= f.fields_for :description, @idea.description do |f| %>
  <%= f.text_area :content %>
  <% end %>

  <%= f.fields_for :problem, @idea.problem do |f| %>
  <%= f.text_area :content %>
  <% end %>

  <%= f.fields_for :solution, @idea.solution do |f| %>
  <%= f.text_area :content %>
  <% end %>

  <%= f.fields_for :market, @idea.market do |f| %>
  <%= f.text_area :content %>
  <% end %>

  <%= f.fields_for :competition, @idea.competition do |f| %>
  <%= f.text_area :content %>
  <% end %>

  <%= f.fields_for :demand, @idea.demand do |f| %>
  <%= f.text_area :content %>
  <% end %>

<%= f.submit %>

  <% end %>